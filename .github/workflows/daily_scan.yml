name: Run Scanner

on:
  schedule:
    - cron: '0 21 * * 1-5' # Runs Mon-Fri at 4pm CST (21:00 UTC)
  workflow_dispatch:      # Allows manual run button

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11' 

      - name: Install libraries
        run: |
          pip install yfinance pandas requests lxml pandas-datareader scipy numpy

      - name: Run Scanner
        env:
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: python main.py
